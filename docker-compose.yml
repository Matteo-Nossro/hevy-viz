services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      # Transmis au script 04_set_password.sh pour ALTER ROLE authenticator
      PGRST_AUTHENTICATOR_PASSWORD: ${PGRST_AUTHENTICATOR_PASSWORD}
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

  postgrest:
    image: postgrest/postgrest:v12.2.0
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      PGRST_DB_URI: postgres://authenticator:${PGRST_AUTHENTICATOR_PASSWORD}@db:5432/${POSTGRES_DB}
      PGRST_DB_SCHEMAS: api
      # Toutes les requêtes assument le rôle 'anon'. L'identité réelle est portée
      # par le header Authorization: Bearer <session_token>, validé par
      # api.current_user_id() contre la table api.sessions (cf. RLS policies).
      PGRST_DB_ANON_ROLE: anon
      PGRST_DB_MAX_ROWS: "1000"
      PGRST_OPENAPI_SERVER_PROXY_URI: http://localhost:${HTTP_PORT}/api

  frontend:
    image: ghcr.io/matteo-nossro/hevy-viz-frontend:latest
    build: ./frontend
    restart: unless-stopped
    depends_on:
      - postgrest
    ports:
      - "${HTTP_PORT}:80"

  adminer:
    image: adminer:standalone
    restart: unless-stopped
    depends_on:
      - db
    environment:
      ADMINER_DEFAULT_SERVER: db
      ADMINER_DESIGN: dracula
    ports:
      # Bind loopback uniquement : Adminer accessible via SSH tunnel ou
      # depuis le LXC, jamais exposé publiquement (VUL-03).
      - "127.0.0.1:${ADMINER_PORT}:8080"

volumes:
  db_data:
    external: true
    name: 21_db_data
